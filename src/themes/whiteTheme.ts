import { createTheme } from '@mui/material/styles';


export const whiteTheme = createTheme({
  palette: {
    primary: {
      light: '#b4d6e4',
      main: '#ffffff',
      contrastText: '#000',
    }
  },
});
